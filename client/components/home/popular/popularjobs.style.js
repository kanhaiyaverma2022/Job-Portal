import { StyleSheet } from "react-native";

import { FONT, SIZES, COLORS } from "../../../constants";

const styles = {
  container:" mt-xLarge",
  header: "flex flex-row justify-between items-center",
  headerTitle: " text-large text-primary font-medium" ,
  headerBtn: "text-medium font-medium text-gray",
  cardsContainer:"mt-medium",
};

export default styles;
